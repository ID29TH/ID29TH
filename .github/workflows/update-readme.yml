name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ID29TH/ID29TH@main@commit-username-and-email
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
